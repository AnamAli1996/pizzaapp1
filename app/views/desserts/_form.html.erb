<%= bootstrap_form_for(@dessert) do |f| %>
  <% if @dessert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dessert.errors.count, "error") %> prohibited this dessert from being saved:</h2>

      <ul>
      <% @dessert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   
    <%= f.text_field :dessert_name %>
  </div>
  <div class="field">

    <%= f.text_field :price %>
  </div>
  <div class="field">

    <%= f.text_field :description %>
  </div>
  <div class="field">
    
    <%= f.check_box :vegetarian %>
  </div>
  <div class="field">

    <%= f.check_box :gluten_free %>
  </div>
  <div class="field">
 
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
